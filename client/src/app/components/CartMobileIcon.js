import { BsHandbagFill } from 'react-icon/bs'


const CartMobileIcon = () => {
    return ( <
        div className = 'bg-tertiary w-[72px] h-[72px] rounded-full flex justify-center items-center text-white
        cursor - pointer fixed right - [10 % ] botton - [5 % ] z - 20 '> <
        BsHandbagFill className = 'text-4xl' / > {} <
        span className = 'absolute text-white bottom-3 right-4 gradient' > 3 < /span> <
        /div>

    );
};

export default CartMobileIcon;